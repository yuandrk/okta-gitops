# AGENTS.md

This repo's full agent guidance lives in **CLAUDE.md** at the repo root — read that file first.

Despite the name it is tool-agnostic: target org, credentials, architecture, the
`terraform plan` before `apply` workflow rule, the drift-reconciliation procedure,
state layout and CI/CD are all documented there.

Keep this file a pointer. Do not copy CLAUDE.md's contents here — an earlier duplicate
drifted out of sync and had to be removed.
